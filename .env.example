# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Host Configuration (auto-detected if not specified)
# HOST_IP=192.168.1.100

# Development Configuration (auto-generated if not specified)
# DEV_API_URL=http://auto-detected-ip:9003/api
# DEV_ALLOWED_ORIGINS=http://auto-detected-ip:9002,http://auto-detected-ip:5173

# Production Configuration (set these for production deployment)
# PROD_API_URL=https://yourdomain.com/api
# PROD_ALLOWED_ORIGINS=https://yourdomain.com

# Node Environment
NODE_ENV=development
